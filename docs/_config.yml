# GitHub Pages + Jekyll — see GITHUB_PAGES.md
# If the site loads without CSS, set baseurl to "/YOUR-REPO-NAME" (must match the GitHub repo name).
title: Orderbook
description: Technical documentation — client-delivered simulated exchange backend, matching engine, scaling paths, Go & Rust, Postgres, observability
theme: jekyll-theme-cayman

baseurl: "/orderbook"
url: "https://mehdishariati.github.io"

plugins:
  - jekyll-relative-links
  - jekyll-github-metadata

markdown: kramdown

exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
