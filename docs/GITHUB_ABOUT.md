# GitHub “About” text (copy-paste)

The repository **description** and **website** are edited on GitHub, not in git:

**Repository page** → right sidebar **About** → **⚙️** (gear) **Edit repository details**

---

## Description (max ~350 characters)

**Short:**

> Simulated exchange backend: Go API, optional Rust matcher, Postgres, Redis, NATS, Prometheus. Price–time matching, REST + WebSocket. Docs in `/docs` (architecture, scaling, tradeoffs).

**One line:**

> Go + optional Rust order book, Postgres, observability — portfolio project. Documentation: `/docs`.

Replace or extend with your own emphasis; stay under the character limit shown in the UI.

---

## Website URL

Paste **one** of:

| What | Example (replace `USER` / `REPO`) |
|------|-----------------------------------|
| Docs folder (always works) | `https://github.com/USER/REPO/tree/main/docs` |
| Docs README | `https://github.com/USER/REPO/blob/main/docs/README.md` |
| GitHub Pages (after you enable it) | `https://USER.github.io/REPO/` |

---

## Topics (optional)

Examples: `go`, `rust`, `postgresql`, `redis`, `nats`, `prometheus`, `orderbook`, `matching-engine`, `websocket`, `docker`
