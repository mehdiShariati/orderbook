# GitHub Pages + Jekyll — see GITHUB_PAGES.md
# If the site loads without CSS, set baseurl to "/YOUR-REPO-NAME" (must match the GitHub repo name).
title: Orderbook
description: Simulated exchange backend — matching engine, Go API, optional Rust matcher, Postgres, observability
theme: jekyll-theme-cayman

baseurl: "/orderbook"
url: "https://mehdishariati.github.io"

plugins:
  - jekyll-relative-links
  - jekyll-github-metadata

markdown: kramdown

exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
